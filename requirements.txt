# HuggingFace Spaces requirements
# Installs the satfinder package from local source

# Core Gradio app dependencies (lightweight subset for HF Spaces)
gradio>=4.0.0
fastapi>=0.100
uvicorn>=0.23
numpy>=1.24.0
pillow>=10.0.0

# Install local package (src-layout)
-e .
